# 第一单元覆盖检查（函数、极限与连续）

> 依据 `unit_1_outline.md` 考纲条目，结合当前 `math_records.json` 已完成题目进行覆盖判定。

## 覆盖结论（更新后）
第一单元考纲点已全部触达，达到“可进入下一单元”的覆盖条件；但以下子项掌握度仍偏弱，建议在进入第二单元后保留每轮 1 题回顾：

- 隐函数局部可解条件与隐函数求导（尤其是 `F_y\neq0` 与 `y'=-F_x/F_y`）。
- 数列极限经典模型 `\lim_{n\to\infty}(1+1/n)^n=e` 及参数变式。
- 夹逼准则的标准不等式构造。

## 已覆盖清单
1. 函数概念与性质
   - 奇偶性、有界性、单调性判定。
2. 复合函数与反函数
   - `g\circ h`、`h\circ g` 计算与定义域判断（已触达，需继续熟练）。
3. 隐函数
   - 以二元方程定义隐函数并进行求导（已触达，当前薄弱）。
4. 极限核心方法
   - 等价无穷小、泰勒展开、极限四则运算。
5. 两个重要极限
   - `\lim_{x\to0}\frac{\sin x}{x}=1`（已熟练）。
   - `\lim_{n\to\infty}(1+1/n)^n=e` 及变式（已触达，当前薄弱）。
6. 极限存在准则
   - 夹逼准则（已触达，当前薄弱）。
7. 连续与间断
   - 补点连续、跳跃间断识别。
8. 闭区间连续函数性质
   - 最大最小值与介值定理应用。

## 单元完成判定
- 第一单元：**完成（允许进入第二单元）**。
